# Development Dockerfile for Eyes App Backend
FROM node:18-alpine

# Create app directory
WORKDIR /usr/src/app

# Install all dependencies (including devDependencies)
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose port
EXPOSE 5000

# Start the application in development mode with nodemon
CMD ["npm", "run", "dev"]
